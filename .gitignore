# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea
.tabbit*
run-tabbit.bat

# AI agent config. Machine-local: accumulates absolute paths, usernames and
# one-off allowed commands, none of it useful to another checkout. Project
# instructions live in CLAUDE.md at the repo root, which stays tracked.
.claude/

# Local env files
.env
.env.*
!.env.example
!.env.docker.example


# runtime files
runtime/uploads/*
!runtime/uploads/.gitkeep
!runtime/uploads/welcome.png
runtime/db/*
!runtime/db/.gitkeep
